import { FuturePage } from "@/components/future-page";

export default function LoginPage() {
  return (
    <FuturePage
      title="Login"
      description="Secure participant and client login will be introduced with role-based access as the platform expands."
    />
  );
}
