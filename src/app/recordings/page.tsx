import { FuturePage } from "@/components/future-page";

export default function RecordingsPage() {
  return (
    <FuturePage
      title="Recordings"
      description="Participant access to session replays and integration notes will be available in this private area."
    />
  );
}
